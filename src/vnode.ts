/**
 * A node tree that can be consumed by an agent to produce a platform independent UI representation.
 * Theoretically could be meaningfully consumed by non-UI agents.
 */
export type VNode = string | number | null;