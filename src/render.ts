import { VNode } from "./vnode";

/**
 * Render JSX into a `parent` element.
 * @param vnode A (JSX) VNode to render.
 * @param parent DOM element to render into.
 */
export function render(vnode: VNode, parent: HTMLElement) {
    
}

/**
 * Render JSX into *(error, decision required)* for a static pre-rendered page.
 */
export function renderIsomorphic() {}


